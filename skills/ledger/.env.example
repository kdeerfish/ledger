# Ledger Skills 配置
# 指向 Ledger Web 服务的 API 地址
#
# 场景：
#   Docker 在本机      → http://127.0.0.1:5800（默认值，可不配置）
#   Docker 在 NAS 上   → http://<NAS-IP>:5800（如 http://192.168.31.126:5800）
#   Docker 在 Windows  → 留空，WSL 自动探测网关

#LEDGER_API_URL=http://192.168.31.126:5800
