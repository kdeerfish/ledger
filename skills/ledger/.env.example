# Ledger Skills 配置
# 指向 Ledger Web 服务的 API 地址
#
# 解析优先级（高→低）：
#   1. 系统环境变量 LEDGER_API_URL（最高优先级）
#   2. 本文件中的 LEDGER_API_URL
#   3. Docker 容器内自动检测 → http://host.docker.internal:5800
#   4. 默认值 → http://127.0.0.1:5800
#
# 常见场景：
#   Docker 在本机       → 不需要配置（默认 127.0.0.1:5800）
#   Docker 在 NAS/远程  → http://<NAS-IP>:5800（如 http://192.168.31.126:5800）
#   AI 在 Docker 容器内 → 自动使用 host.docker.internal（无需配置）
#   Docker 在 Windows   → 留空，自动探测网关

#LEDGER_API_URL=http://192.168.31.126:5800
