# ── Go ─────────────────────────────────────────────
bin/
dist/
*.test
*.out
coverage.out
coverage.html

# ── 工具链 / 依赖锁文件保留 ────────────────────────
# (go.sum 必须提交)

# ── 本地构建产物 / 部署目录 ───────────────────────
deploy/
tools.go

# ── 数据库 (用户自有数据) ─────────────────────────
ledger.db
*.db
*.db-wal
*.db-shm

# ── 环境变量 (含密钥) ──────────────────────────────
.env
.env.local

# ── 临时文件 ──────────────────────────────────────
*.tmp
*.bak
.DS_Store
Thumbs.db

# ── 前端构建产物 / 依赖 ──────────────────────────
frontend/node_modules/
frontend/dist/
website/node_modules/
website/build/
website/.docusaurus/

# ── IDE / 工具配置 ────────────────────────────────
.vscode/
.idea/
*.swp
.codegraph/
.reasonix/
reasonix.toml

# ── 旧 Python 残留 (分支清理后不再产生) ─────────
__pycache__/
*.py[cod]
.pytest_cache/
htmlcov/

# ── Docusaurus 旧 .nojekyll(已迁到 website/static) ──
/.nojekyll
