# Ledger 服务配置（留空使用默认值）
# 复制此文件为 .env 进行自定义配置

# 数据库文件路径（默认：./ledger.db）
# LEDGER_DB_PATH=

# Web 服务配置
# WEB_HOST=0.0.0.0
# WEB_PORT=5800
# WEB_DEBUG=false

# CORS 跨域设置（默认只允许同源访问，留空 = 禁止跨域）
# 如需从其他域名访问 API，填允许的域名，逗号分隔
# WEB_CORS_ORIGINS=https://myapp.example.com
